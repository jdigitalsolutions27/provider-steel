import { CenterLoader } from "@/components/ui/center-loader";

export default function AdminLoading() {
  return (
    <CenterLoader title="Loading Dashboard" subtitle="Gathering leads and updates..." />
  );
}
